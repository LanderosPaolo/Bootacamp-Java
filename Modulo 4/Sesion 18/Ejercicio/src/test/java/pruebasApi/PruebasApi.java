package pruebasApi;
import org.example.service.ApiService;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;


/*@DisplayName("Test para el consumo de API")
public class PruebasApi {

    private final ApiService apiService = new ApiService();
    @Test
    @DisplayName("Test de la conexion a la Api.")
    void testConexion() {
        assertEquals(true, apiService.obtenerConexion());
    }

    @Test
    @DisplayName("Test del status de la conexion de la Api.")
    void testStatus() {
        assertEquals(200, apiService.obtenerStatus());
    }

    @Test
    @DisplayName("Test de la Latitud.")
    void testLatitud() {
        assertEquals(apiService.obtenerLatitud(), apiService.obtenerLatitud());
    }

    @Test
    @DisplayName("Test de la Longitud.")
    void testLongitud() {
        assertEquals(apiService.obtenerLongitud(), apiService.obtenerLongitud());
    }

    @Test
    @DisplayName("Test del ID de la conexion")
    void testId() {
        assertEquals(apiService.obtenerIdConexion(), apiService.obtenerIdConexion());
    }
}*/
